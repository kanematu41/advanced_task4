			<%= form_with(model: [book, comment]) do |f| %>
				<%= f.text_area :comment, rows:"3", class: "form-control", placeholder: "コメントここに" %>
				<%= f.submit "送信する", class:"pull-right" %>
			<% end %>